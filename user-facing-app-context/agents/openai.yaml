interface:
  display_name: "User-Facing App Context"
  short_description: "Reusable app layout and design context."
  default_prompt: "Use $user-facing-app-context with $backend-setup to scaffold a polished user-facing web app shell without copying domain-specific pipeline logic."
